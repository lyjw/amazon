<% if review.liked_by(current_user) %>
  <%= link_to "Unlike", product_review_like_path(@product, review, review.liked_by(current_user)), method: :delete, remote: true %>
<% else %>
  <%= link_to "Like", product_review_likes_path(@product, review), method: :post, remote: true %>
<% end %>
