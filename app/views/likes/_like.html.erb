<% if review.liked_by(current_user) %>
  <%= link_to "Unlike", review_like_path(review, review.liked_by(current_user)), method: :delete, remote: true %>
<% else %>
  <%= link_to "Like", review_likes_path(review), method: :post, remote: true %>
<% end %>
