<% if user_favourite(product) %>
  <%= link_to icon("heart"), product_favourite_path(product, user_favourite(product)), method: :delete, remote: true %>
<% else %>
  <%= link_to icon("heart-o"), product_favourites_path(product), method: :post, remote: true %>
<% end %>
